# MedScope AI — Python dependencies
# Install with: pip install -r requirements.txt

# Core
flask==3.0.3
flask-cors==5.0.0

# PDF extraction (install at least one)
PyMuPDF==1.24.10        # primary — faster
pdfplumber==0.11.4      # fallback

# AI APIs (optional — only install the one you use)
# google-generativeai==0.8.3   # for Gemini
# openai==1.51.2               # for OpenAI GPT
