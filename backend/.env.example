# Copy this file to .env and fill in your keys

# Optional: Google Gemini (recommended for hackathon — free tier available)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: OpenAI GPT
# OPENAI_API_KEY=your_openai_api_key_here

# If neither key is set, the backend uses the built-in rule-based extractor (demo mode)
